// (User) Problem:
// We have: two numbers
// We need: if they are equal
// We must:
// fuction name: isEqual
// omit argument labels using _
// Solution (Product):
// return a boolean 1 == 2

func isEqual(_ number1: Int, _ number2: Int) -> Bool {
    return number1 == number2
}
